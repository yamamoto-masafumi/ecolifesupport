// jQuery(function($) {
//     if(window.innerWidth >= 768){
//         var win = $(window),
//             list = $('#usefularticlelist'),
//             listPos = list.offset().top,
//             fixedClass = 'is-fixed2';

//         win.on('load scroll', function() {
//             var value = $(this).scrollTop();
//             if ( value > listPos ) {
//             list.addClass(fixedClass);
//             } else {
//             list.removeClass(fixedClass);
//             }
//         });
//     }
//   });
